-- ==================== MISSION HUNTER ESP - RAYFIELD VERSION ====================
-- Features: Player/NPC ESP, Walkspeed Multiplier, Teleport Systems
-- Keybind: Right Ctrl to toggle

-- Load Rayfield Library
local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local TeleportService = game:GetService("TeleportService")
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

-- Configuration
local Config = {
    Enabled = true,
    ShowNames = true,
    ShowBars = true,
    ShowNPCs = true,
    ShowValues = true,
    ShowWalkspeed = true,
    WalkspeedMultiplier = 1.0,
    MaxDistance = 1500,
    TextSize = 14,
    BarWidth = 150,
    BarHeight = 6,
    YOffset = 60
}

-- State
local ESP_Cache = {}
local LiveFolder = workspace:FindFirstChild("Live")
local BaseWalkspeed = 16
local WalkspeedUpdateConnection = nil

-- Function to get WalkSpeed attribute
local function GetBaseWalkspeedFromAttribute(humanoid)
    local attrSpeed = humanoid:GetAttribute("WalkSpeed")
    if attrSpeed then return attrSpeed end
    return humanoid.WalkSpeed
end

-- Apply walkspeed multiplier
local function ApplyWalkspeedMultiplier()
    local character = LocalPlayer.Character
    if not character then return end
    
    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then return end
    
    if Config.ShowWalkspeed then
        local baseSpeed = GetBaseWalkspeedFromAttribute(humanoid)
        local targetSpeed = baseSpeed * Config.WalkspeedMultiplier
        
        if math.abs(humanoid.WalkSpeed - targetSpeed) > 0.1 then
            humanoid.WalkSpeed = targetSpeed
        end
    end
end

-- Start walkspeed monitoring
local function StartWalkspeedMonitoring()
    if WalkspeedUpdateConnection then
        WalkspeedUpdateConnection:Disconnect()
        WalkspeedUpdateConnection = nil
    end
    
    local character = LocalPlayer.Character
    if not character then return end
    
    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then return end
    
    BaseWalkspeed = GetBaseWalkspeedFromAttribute(humanoid)
    
    WalkspeedUpdateConnection = RunService.RenderStepped:Connect(function()
        if not Config.Enabled or not Config.ShowWalkspeed then return end
        
        local currentChar = LocalPlayer.Character
        if not currentChar then return end
        
        local currentHumanoid = currentChar:FindFirstChild("Humanoid")
        if not currentHumanoid then return end
        
        local currentBase = GetBaseWalkspeedFromAttribute(currentHumanoid)
        local currentSpeed = currentHumanoid.WalkSpeed
        local targetSpeed = currentBase * Config.WalkspeedMultiplier
        
        if math.abs(currentSpeed - targetSpeed) > 0.1 then
            currentHumanoid.WalkSpeed = targetSpeed
        end
    end)
end

-- Character respawn handling
local function OnCharacterAdded(character)
    local humanoid = character:WaitForChild("Humanoid")
    task.wait(0.1)
    
    if Config.ShowWalkspeed then
        humanoid.WalkSpeed = GetBaseWalkspeedFromAttribute(humanoid) * Config.WalkspeedMultiplier
    end
    
    StartWalkspeedMonitoring()
end

LocalPlayer.CharacterAdded:Connect(OnCharacterAdded)
if LocalPlayer.Character then
    OnCharacterAdded(LocalPlayer.Character)
end

-- Death cleanup
LocalPlayer.CharacterRemoving:Connect(function()
    for model, entry in pairs(ESP_Cache) do
        if entry.Drawings then
            for _, drawing in pairs(entry.Drawings) do
                pcall(function() 
                    if drawing then
                        drawing.Visible = false
                        drawing:Remove() 
                    end
                end)
            end
        end
    end
    ESP_Cache = {}
end)

-- ==================== ESP SYSTEM ====================

local function CreateDrawing(type, props)
    local obj = Drawing.new(type)
    for k, v in pairs(props) do obj[k] = v end
    return obj
end

local function RemoveESP(model)
    if ESP_Cache[model] then
        for _, drawing in pairs(ESP_Cache[model].Drawings) do
            drawing:Remove()
        end
        ESP_Cache[model] = nil
    end
end

-- ESP Render Loop
RunService.RenderStepped:Connect(function()
    if not Config.Enabled then 
        for model, _ in pairs(ESP_Cache) do RemoveESP(model) end
        return 
    end

    if not LiveFolder then 
        LiveFolder = workspace:FindFirstChild("Live") 
        return 
    end

    if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        for model, _ in pairs(ESP_Cache) do RemoveESP(model) end
        return
    end

    for _, entry in pairs(ESP_Cache) do 
        entry.Seen = false 
    end

    for _, folder in ipairs(LiveFolder:GetChildren()) do
        local model = folder:IsA("Model") and folder or folder:FindFirstChildWhichIsA("Model")
        
        if model and model ~= LocalPlayer.Character then
            
            local head = model:FindFirstChild("Head")
            local humanoid = model:FindFirstChild("Humanoid")
            local player = Players:GetPlayerFromCharacter(model)

            if player and humanoid then
                pcall(function()
                    humanoid.HealthDisplayType = Enum.HumanoidHealthDisplayType.AlwaysOn
                end)
            end

            local isNPC = not player

            if isNPC and not Config.ShowNPCs then
                if ESP_Cache[model] then RemoveESP(model) end
                continue
            end

            if head and humanoid then
                if not ESP_Cache[model] then
                    ESP_Cache[model] = {
                        Seen = true,
                        Drawings = {
                            Name = CreateDrawing("Text", {Center = true, Outline = true, Color = Color3.new(1,1,1), Size = Config.TextSize}),
                            Bar1Bg = CreateDrawing("Square", {Filled = true, Color = Color3.new(0.1,0.1,0.1), Transparency = 0.8}),
                            Bar1Fill = CreateDrawing("Square", {Filled = true, Transparency = 1}),
                            Bar1Text = CreateDrawing("Text", {Center = false, Outline = true, Color = Color3.new(1,1,1), Size = 13}),
                            Bar2Bg = CreateDrawing("Square", {Filled = true, Color = Color3.new(0.1,0.1,0.1), Transparency = 0.8}),
                            Bar2Fill = CreateDrawing("Square", {Filled = true, Transparency = 1}),
                            Bar2Text = CreateDrawing("Text", {Center = false, Outline = true, Color = Color3.new(1,1,1), Size = 13}),
                        }
                    }
                end

                local entry = ESP_Cache[model]
                entry.Seen = true
                local d = entry.Drawings

                local vector, onScreen = Camera:WorldToViewportPoint(head.Position)
                local distance = (Camera.CFrame.Position - head.Position).Magnitude

                if onScreen and distance <= Config.MaxDistance then
                    
                    local val1, min1, max1, color1 = 0, 0, 100, Color3.new(1, 0, 0)
                    local val2, min2, max2, color2 = 0, 0, 100, Color3.new(1, 1, 0)
                    local showBar2 = false
                    local cleanName = ""

                    if isNPC then
                        val1 = humanoid.Health
                        max1 = humanoid.MaxHealth
                        min1 = 0
                        color1 = Color3.new(0.2, 0.9, 0.2)
                        showBar2 = false
                        cleanName = ""
                    else
                        cleanName = (player.DisplayName and player.DisplayName ~= "" and player.DisplayName) or player.Name

                        local lf = folder:FindFirstChild("Lifeforce") or model:FindFirstChild("Lifeforce")
                        if lf then 
                            val1 = lf.Value 
                            max1 = lf:GetAttribute("MaxValue") or lf.MaxValue or 100
                            min1 = lf:GetAttribute("MinValue") or lf.MinValue or 0
                        else
                            val1 = humanoid.Health
                            max1 = humanoid.MaxHealth
                            min1 = 0
                        end
                        
                        local pos = folder:FindFirstChild("Posture") or model:FindFirstChild("Posture")
                        if pos then 
                            val2 = pos.Value 
                            max2 = pos:GetAttribute("MaxValue") or pos.MaxValue or 100
                            min2 = pos:GetAttribute("MinValue") or pos.MinValue or 0
                            showBar2 = true
                        end
                    end
                    
                    local range1 = max1 - min1
                    local pct1 = range1 > 0 and math.clamp((val1 - min1) / range1, 0, 1) or 0
                    
                    local range2 = max2 - min2
                    local pct2 = range2 > 0 and math.clamp((val2 - min2) / range2, 0, 1) or 0

                    local screenPos = Vector2.new(vector.X, vector.Y)
                    local startY = screenPos.Y - Config.YOffset
                    local currentY = startY

                    if Config.ShowNames and not isNPC and cleanName ~= "" then
                        d.Name.Visible = true
                        d.Name.Text = cleanName
                        d.Name.Position = Vector2.new(screenPos.X, startY - 30)
                        d.Name.Size = Config.TextSize
                    else
                        d.Name.Visible = false
                    end

                    if Config.ShowBars then
                        local bar1Pos = Vector2.new(screenPos.X - (Config.BarWidth / 2), currentY)
                        
                        d.Bar1Bg.Visible = true
                        d.Bar1Bg.Size = Vector2.new(Config.BarWidth, Config.BarHeight)
                        d.Bar1Bg.Position = bar1Pos
                        d.Bar1Fill.Visible = true
                        d.Bar1Fill.Size = Vector2.new(Config.BarWidth * pct1, Config.BarHeight)
                        d.Bar1Fill.Position = bar1Pos
                        d.Bar1Fill.Color = color1

                        if Config.ShowValues then
                            d.Bar1Text.Visible = true
                            d.Bar1Text.Text = string.format("%.0f/%.0f", val1, max1)
                            d.Bar1Text.Position = bar1Pos + Vector2.new(Config.BarWidth + 5, -3)
                        else
                            d.Bar1Text.Visible = false
                        end
                        
                        currentY = currentY - (Config.BarHeight + 2)
                        
                        if showBar2 and not isNPC then
                            local bar2Pos = Vector2.new(screenPos.X - (Config.BarWidth / 2), currentY)
                            
                            d.Bar2Bg.Visible = true
                            d.Bar2Bg.Size = Vector2.new(Config.BarWidth, Config.BarHeight)
                            d.Bar2Bg.Position = bar2Pos
                            d.Bar2Fill.Visible = true
                            d.Bar2Fill.Size = Vector2.new(Config.BarWidth * pct2, Config.BarHeight)
                            d.Bar2Fill.Position = bar2Pos
                            d.Bar2Fill.Color = color2

                            if Config.ShowValues then
                                d.Bar2Text.Visible = true
                                d.Bar2Text.Text = string.format("%.0f/%.0f", val2, max2)
                                d.Bar2Text.Position = bar2Pos + Vector2.new(Config.BarWidth + 5, -3)
                            else
                                d.Bar2Text.Visible = false
                            end
                        else
                            d.Bar2Bg.Visible = false
                            d.Bar2Fill.Visible = false
                            d.Bar2Text.Visible = false
                        end
                    else
                        d.Bar1Bg.Visible = false
                        d.Bar1Fill.Visible = false
                        d.Bar1Text.Visible = false
                        d.Bar2Bg.Visible = false
                        d.Bar2Fill.Visible = false
                        d.Bar2Text.Visible = false
                    end

                else
                    for _, v in pairs(d) do 
                        if v and v.Visible ~= nil then
                            v.Visible = false 
                        end
                    end
                end
            end
        end
    end

    for model, entry in pairs(ESP_Cache) do
        if not entry.Seen or not model or not model.Parent then
            RemoveESP(model)
        end
    end
end)

-- ==================== CREATE RAYFIELD WINDOW ====================

local Window = Rayfield:CreateWindow({
    Name = "Mission Hunter ESP",
    LoadingTitle = "Mission Hunter ESP",
    LoadingSubtitle = "by System",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "MissionHunterESP",
        FileName = "Config.json"
    },
    Discord = {
        Enabled = false,
        Invite = "",
        RememberJoins = true
    },
    KeySystem = false,
    KeySettings = {
        Title = "Key System",
        Subtitle = "Enter Key",
        Note = "No key required",
        FileName = "Key",
        SaveKey = false,
        GrabKeyFromSite = false,
        Key = {"Hello"}
    }
})

-- ==================== MAIN TAB ====================

local MainTab = Window:CreateTab("Main", 4483362458)
local MainSection = MainTab:CreateSection("ESP Settings")

-- Master toggle
MainTab:CreateToggle({
    Name = "Master Enabled",
    CurrentValue = true,
    Flag = "MasterEnabled",
    Callback = function(Value)
        Config.Enabled = Value
    end
})

MainTab:CreateToggle({
    Name = "Show Player Names",
    CurrentValue = true,
    Flag = "ShowNames",
    Callback = function(Value)
        Config.ShowNames = Value
    end
})

MainTab:CreateToggle({
    Name = "Show NPCs",
    CurrentValue = true,
    Flag = "ShowNPCs",
    Callback = function(Value)
        Config.ShowNPCs = Value
    end
})

MainTab:CreateToggle({
    Name = "Show Health Bars",
    CurrentValue = true,
    Flag = "ShowBars",
    Callback = function(Value)
        Config.ShowBars = Value
    end
})

MainTab:CreateToggle({
    Name = "Show Bar Values",
    CurrentValue = true,
    Flag = "ShowValues",
    Callback = function(Value)
        Config.ShowValues = Value
    end
})

MainTab:CreateToggle({
    Name = "Enable Walkspeed Multiplier",
    CurrentValue = true,
    Flag = "ShowWalkspeed",
    Callback = function(Value)
        Config.ShowWalkspeed = Value
        if Value then
            ApplyWalkspeedMultiplier()
            StartWalkspeedMonitoring()
        else
            local character = LocalPlayer.Character
            if character then
                local humanoid = character:FindFirstChild("Humanoid")
                if humanoid then
                    humanoid.WalkSpeed = GetBaseWalkspeedFromAttribute(humanoid)
                end
            end
            if WalkspeedUpdateConnection then
                WalkspeedUpdateConnection:Disconnect()
                WalkspeedUpdateConnection = nil
            end
        end
    end
})

local SliderSection = MainTab:CreateSection("Slider Settings")

MainTab:CreateSlider({
    Name = "Walkspeed Multiplier",
    Range = {0.5, 5},
    Increment = 0.1,
    Suffix = "x",
    CurrentValue = 1.0,
    Flag = "WalkspeedSlider",
    Callback = function(Value)
        Config.WalkspeedMultiplier = Value
        if Config.ShowWalkspeed then
            ApplyWalkspeedMultiplier()
        end
    end
})

MainTab:CreateSlider({
    Name = "Max Distance",
    Range = {100, 5000},
    Increment = 10,
    Suffix = "studs",
    CurrentValue = 1500,
    Flag = "MaxDistance",
    Callback = function(Value)
        Config.MaxDistance = Value
    end
})

MainTab:CreateSlider({
    Name = "Text Size",
    Range = {8, 24},
    Increment = 1,
    Suffix = "px",
    CurrentValue = 14,
    Flag = "TextSize",
    Callback = function(Value)
        Config.TextSize = Value
    end
})

MainTab:CreateSlider({
    Name = "Bar Width",
    Range = {50, 300},
    Increment = 5,
    Suffix = "px",
    CurrentValue = 150,
    Flag = "BarWidth",
    Callback = function(Value)
        Config.BarWidth = Value
    end
})

MainTab:CreateSlider({
    Name = "Bar Height",
    Range = {2, 20},
    Increment = 1,
    Suffix = "px",
    CurrentValue = 6,
    Flag = "BarHeight",
    Callback = function(Value)
        Config.BarHeight = Value
    end
})

MainTab:CreateSlider({
    Name = "Vertical Offset",
    Range = {20, 200},
    Increment = 5,
    Suffix = "px",
    CurrentValue = 60,
    Flag = "YOffset",
    Callback = function(Value)
        Config.YOffset = Value
    end
})

-- ==================== TELEPORT TAB ====================

local TeleportTab = Window:CreateTab("Teleport", 4483362458)
local InGameSection = TeleportTab:CreateSection("In-Game Teleport")

-- Define world locations
local WorldTeleports = {
    [14220581641] = {
        name = "Forest World",
        locations = {
            "Spawn Area:0,50,0",
            "Train Station:636,17,1730",
            "Temari & Arrow Boss:1067,20,384",
            "Rui Boss:499,12,-1076",
            "Final Selection:-729,284,-432",
            "Holiday Boss:-1293,273,63",
            "Wind & Snake Boss:2045,136,1684",
            "Kill Grip Spot:-20,155,1137",
        }
    },
    [14220581261] = {
        name = "Tokyo World",
        locations = {
            "Slayer Base:1879,27,797",
            "Train Station:-1178,9,-472",
            "Forest Gate:-861,9,1269",
            "Shop:-436,20,-516",
        }
    },
    [14220581884] = {
        name = "Plains World",
        locations = {
            "Slayer Base:-1819,117,45",
            "Thunder Boss:1454,303,2037",
            "Kill Spot:1675,320,174",
        }
    },
    [14220582083] = {
        name = "Infinity Fortress",
        locations = {
            "Secret Spot:-737,-1262,-2516",
        }
    },
}

-- Teleport function
local function TeleportTo(position)
    local player = LocalPlayer
    if not player.Character then
        Rayfield:Notify({Title = "Error", Content = "No character found!", Duration = 3})
        return
    end
    
    local root = player.Character:FindFirstChild("HumanoidRootPart")
    if not root then
        Rayfield:Notify({Title = "Error", Content = "No HumanoidRootPart!", Duration = 3})
        return
    end
    
    root.CFrame = CFrame.new(position)
    Rayfield:Notify({Title = "Teleported", Content = "Teleported to location!", Duration = 2})
end

-- Add buttons for current world
local currentWorld = WorldTeleports[game.PlaceId]
if currentWorld then
    TeleportTab:CreateLabel("Current World: " .. currentWorld.name)
    
    for _, locString in ipairs(currentWorld.locations) do
        local name, x, y, z = locString:match("([^:]+):([^,]+),([^,]+),(.+)")
        local pos = Vector3.new(tonumber(x), tonumber(y), tonumber(z))
        
        TeleportTab:CreateButton({
            Name = "📍 " .. name,
            Callback = function()
                TeleportTo(pos)
            end
        })
    end
else
    TeleportTab:CreateLabel("No saved locations for this world")
end

-- ==================== SERVER TELEPORT TAB ====================

local ServerTab = Window:CreateTab("Server Teleport", 4483362458)
local ServerSection = ServerTab:CreateSection("World Hopping")

local GameLocations = {
    ["Forest World"] = 14220581641,
    ["Tokyo World"] = 14220581261,
    ["Plains World"] = 14220581884,
    ["Infinity Fortress"] = 14220582083,
}

for name, placeId in pairs(GameLocations) do
    if placeId ~= game.PlaceId then
        ServerTab:CreateButton({
            Name = "🌍 " .. name,
            Callback = function()
                Rayfield:Notify({Title = "Teleporting", Content = "Teleporting to " .. name .. "...", Duration = 2})
                task.wait(0.5)
                TeleportService:Teleport(placeId, LocalPlayer)
            end
        })
    end
end

local CustomSection = ServerTab:CreateSection("Custom Join")

ServerTab:CreateInput({
    Name = "Place ID",
    CurrentValue = "",
    PlaceholderText = "Enter Place ID",
    RemoveTextAfterFocusLost = false,
    Callback = function(Value)
        _G.CustomPlaceId = tonumber(Value)
    end
})

ServerTab:CreateButton({
    Name = "🚀 Teleport to Place ID",
    Callback = function()
        local placeId = _G.CustomPlaceId
        if not placeId then
            Rayfield:Notify({Title = "Error", Content = "Enter a valid Place ID!", Duration = 3})
            return
        end
        Rayfield:Notify({Title = "Teleporting", Content = "Teleporting to Place ID: " .. placeId, Duration = 2})
        task.wait(0.5)
        TeleportService:Teleport(placeId, LocalPlayer)
    end
})

ServerTab:CreateButton({
    Text = "🔄 Rejoin Current Game",
    Func = function()
        Rayfield:Notify({Title = "Rejoining", Content = "Rejoining current game...", Duration = 2})
        task.wait(0.5)
        TeleportService:Teleport(game.PlaceId, LocalPlayer)
    end
})

-- ==================== ADVANCED JOIN TAB ====================

local AdvancedTab = Window:CreateTab("Advanced Join", 4483362458)
local JobIdSection = AdvancedTab:CreateSection("Join by Job ID")

AdvancedTab:CreateInput({
    Name = "Job ID",
    CurrentValue = "",
    PlaceholderText = "Paste Job ID here",
    RemoveTextAfterFocusLost = false,
    Callback = function(Value)
        _G.TargetJobId = Value
    end
})

AdvancedTab:CreateInput({
    Name = "Place ID (optional)",
    CurrentValue = tostring(game.PlaceId),
    PlaceholderText = "Leave blank for current game",
    RemoveTextAfterFocusLost = false,
    Callback = function(Value)
        _G.TargetPlaceId = tonumber(Value) or game.PlaceId
    end
})

AdvancedTab:CreateButton({
    Name = "🚀 JOIN BY JOB ID",
    Callback = function()
        local jobId = _G.TargetJobId
        local placeId = _G.TargetPlaceId or game.PlaceId
        
        if not jobId or jobId == "" then
            Rayfield:Notify({Title = "Error", Content = "Please enter a Job ID!", Duration = 3})
            return
        end
        
        Rayfield:Notify({Title = "Joining", Content = "Joining server...", Duration = 2})
        task.wait(0.5)
        
        pcall(function()
            TeleportService:TeleportToPlaceInstance(placeId, jobId, LocalPlayer)
        end)
    end
})

AdvancedTab:CreateLabel("Current Job ID: " .. game.JobId)

AdvancedTab:CreateButton({
    Name = "📋 Copy Current Job ID",
    Callback = function()
        setclipboard(game.JobId)
        Rayfield:Notify({Title = "Copied", Content = "Job ID copied to clipboard!", Duration = 2})
    end
})

local PlayerJoinSection = AdvancedTab:CreateSection("Join by Player")

AdvancedTab:CreateInput({
    Name = "Username",
    CurrentValue = "",
    PlaceholderText = "Enter player username",
    RemoveTextAfterFocusLost = false,
    Callback = function(Value)
        _G.TargetUsername = Value
    end
})

-- Function to get player server info
local function GetPlayerServerInfo(userId)
    local success, result = pcall(function()
        local url = string.format("https://games.roblox.com/v1/games/%d/players/%d", game.PlaceId, userId)
        local response = game:HttpGet(url)
        return game:GetService("HttpService"):JSONDecode(response)
    end)
    
    if success and result and result.jobId then
        return result.jobId
    end
    return nil
end

AdvancedTab:CreateButton({
    Name = "🔍 FIND PLAYER",
    Callback = function()
        local username = _G.TargetUsername
        if not username or username == "" then
            Rayfield:Notify({Title = "Error", Content = "Please enter a username!", Duration = 3})
            return
        end
        
        local success, userId = pcall(function()
            local url = string.format("https://users.roblox.com/v1/users/search?keyword=%s&limit=1", username)
            local response = game:HttpGet(url)
            local data = game:GetService("HttpService"):JSONDecode(response)
            if data.data and #data.data > 0 then
                return data.data[1].id
            end
            return nil
        end)
        
        if success and userId then
            _G.TargetUserId = userId
            local jobId = GetPlayerServerInfo(userId)
            if jobId then
                _G.FoundJobId = jobId
                Rayfield:Notify({Title = "Found", Content = "Player found! Ready to join.", Duration = 3})
            else
                Rayfield:Notify({Title = "Error", Content = "Player not in this game!", Duration = 3})
            end
        else
            Rayfield:Notify({Title = "Error", Content = "Could not find user!", Duration = 3})
        end
    end
})

AdvancedTab:CreateButton({
    Name = "👥 JOIN PLAYER'S SERVER",
    Callback = function()
        if not _G.FoundJobId then
            Rayfield:Notify({Title = "Error", Content = "Find a player first!", Duration = 3})
            return
        end
        
        Rayfield:Notify({Title = "Joining", Content = "Joining player's server...", Duration = 2})
        task.wait(0.5)
        
        TeleportService:TeleportToPlaceInstance(game.PlaceId, _G.FoundJobId, LocalPlayer)
    end
})

-- ==================== SERVER BROWSER ====================

local BrowserSection = AdvancedTab:CreateSection("Server Browser")

local function FetchServers(placeId)
    local success, result = pcall(function()
        local url = string.format("https://games.roblox.com/v1/games/%d/servers/Public?limit=20", placeId)
        local response = game:HttpGet(url)
        return game:GetService("HttpService"):JSONDecode(response)
    end)
    
    if success and result and result.data then
        return result.data
    end
    return {}
end

AdvancedTab:CreateInput({
    Name = "Place ID to browse",
    CurrentValue = tostring(game.PlaceId),
    PlaceholderText = "Enter Place ID",
    RemoveTextAfterFocusLost = false,
    Callback = function(Value)
        _G.BrowsePlaceId = tonumber(Value) or game.PlaceId
    end
})

local serverOptions = {}
local serverData = {}

AdvancedTab:CreateDropdown({
    Name = "Select Server",
    Options = {"No servers loaded"},
    CurrentOption = "No servers loaded",
    Flag = "ServerDropdown",
    Callback = function(Value)
        if serverData[Value] then
            _G.SelectedServer = serverData[Value]
            Rayfield:Notify({Title = "Selected", Content = "Server selected!", Duration = 2})
        end
    end
})

AdvancedTab:CreateButton({
    Name = "📡 Fetch Active Servers",
    Callback = function()
        local placeId = _G.BrowsePlaceId or game.PlaceId
        Rayfield:Notify({Title = "Fetching", Content = "Getting server list...", Duration = 2})
        
        local servers = FetchServers(placeId)
        serverOptions = {}
        serverData = {}
        
        if #servers == 0 then
            Rayfield:Notify({Title = "No Servers", Content = "No active servers found!", Duration = 3})
            return
        end
        
        for i, server in ipairs(servers) do
            local optionName = string.format("Server %d: %d/%d players", i, server.playing or 0, server.maxPlayers or 0)
            table.insert(serverOptions, optionName)
            serverData[optionName] = server.id
        end
        
        -- Update dropdown (Rayfield doesn't have dynamic dropdown updates, so we'll just notify)
        Rayfield:Notify({Title = "Success", Content = string.format("Found %d servers", #servers), Duration = 3})
        _G.FetchedServers = servers
    end
})

AdvancedTab:CreateButton({
    Name = "🚀 Join Selected Server",
    Callback = function()
        if not _G.SelectedServer then
            Rayfield:Notify({Title = "Error", Content = "No server selected!", Duration = 3})
            return
        end
        
        Rayfield:Notify({Title = "Joining", Content = "Joining server...", Duration = 2})
        task.wait(0.5)
        
        TeleportService:TeleportToPlaceInstance(_G.BrowsePlaceId or game.PlaceId, _G.SelectedServer, LocalPlayer)
    end
})

AdvancedTab:CreateButton({
    Name = "📋 Copy Selected Job ID",
    Callback = function()
        if _G.SelectedServer then
            setclipboard(_G.SelectedServer)
            Rayfield:Notify({Title = "Copied", Content = "Job ID copied!", Duration = 2})
        else
            Rayfield:Notify({Title = "Error", Content = "No server selected!", Duration = 3})
        end
    end
})

-- ==================== INIT ====================

Rayfield:Notify({
    Title = "Mission Hunter ESP",
    Content = "Script Loaded! Press Right Ctrl to toggle menu",
    Duration = 5
})

print("=== Mission Hunter ESP (Rayfield) Loaded ===")
print("Press Right Ctrl to toggle menu")
